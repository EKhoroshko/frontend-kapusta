export const getUserName = (store) => store.auth.name;

export const getUserEmail = (store) => store.auth.email;

export const getIsLoggedIn = (store) => store.auth.isLoggedIn;

export const getUserId = (store) => store.auth.id;
